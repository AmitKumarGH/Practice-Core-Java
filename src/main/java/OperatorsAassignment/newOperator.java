package OperatorsAassignment;

public class newOperator {
    public static void main(String[] args) {
        //new operator :
        //We can use "new" operator to create an object.
        //There is no "delete" operator in java because destruction of useless objects is the
        //responsibility of garbage collector
        newOperator newOper = new newOperator();
    }
}
